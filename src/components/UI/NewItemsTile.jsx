import React from 'react'
import { Link } from "react-router-dom";

export default function NewItemsTile({ authorId, authorImage, nftImage, nftId, title, price, likes, expiryDate }) {
  return (
    <div>NewItemsTile</div>
  )
}
